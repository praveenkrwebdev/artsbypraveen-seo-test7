# ArtsByPraveen — SEO & Page Structure Test

This is a temporary GitHub Pages test build.

- Internal links use relative paths so the site works under a GitHub Pages project URL and on the production custom domain.
- The test build uses `noindex, nofollow` on pages intended for testing.
- `CNAME` is intentionally omitted from this test repository.
- Case studies are intentionally not included yet.

Before moving to production, restore production indexing settings and add the live `CNAME` file.
